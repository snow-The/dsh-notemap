import { GraphStore } from '../src/graph.ts';
const s = new GraphStore(':memory:');
s.addNode({ id: 'a', title: '处女座性格解析', content: '处女座的人自私狭隘,追求完美,害怕寂寞,需要私人空间' });
s.addNode({ id: 'b', title: '天秤座恋爱指南', content: '天秤座优雅浪漫,恋爱中注重平衡,害怕寂寞' });
s.addNode({ id: 'c', title: '彩票开奖公告', content: '双色球第2024001期开奖号码公布,中奖查询' });
const q = '处女座的人自私狭隘,追求完美,害怕寂寞';
console.log('--- searchNodes ---');
console.log(s.searchNodes(q, 5).map(r => r.id + ':' + r.title).join(' | ') || '(empty)');
console.log('--- searchFused ---');
console.log(s.searchFused(q, { limit: 5 }).map(r => r.node.id + ':' + r.node.title + ':' + r.score.toFixed(3)).join(' | ') || '(empty)');
console.log('fts schema:', JSON.stringify(s.db.prepare("SELECT sql FROM sqlite_master WHERE name='nodes_fts'").get()));
console.log('--- short query 处女座 ---');
console.log(s.searchFused('处女座', { limit: 5 }).map(r => r.node.id + ':' + r.node.title).join(' | ') || '(empty)');