# dsh-notemap - infinite-canvas note map for DeepSeek Harness.
#
# Host plugin: registers the notemap_* graph tools (headless - the canvas UI was
# removed in 0.12.0).
- insert:
    - id: dsh-notemap
      name: '@snow-the/dsh-notemap'
